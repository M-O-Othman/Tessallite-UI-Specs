# Session handout

## Project
Tessallite-UI-Specs.

## Session Goal
Housekeep the documentation layout so the repository has one clear lowercase
`docs/` directory instead of parallel `Docs/` and `docs/` folders.

## What Was Done
Before editing, reviewed `AGENTS.md`, `work/action-plan.md`, the latest session
handout, the documentation plan and all current documentation paths. The
housekeeping plan keeps `docs/` as the canonical home for guides, issue logs,
indexes, screenshots and questions.

## What Was Tried and Failed
No cleanup command has failed. The real-browser verification remains blocked
by the unavailable Browser connection from the earlier viewer work; this
housekeeping task does not waive that evidence requirement.

## Current State
One lowercase `docs/` tree now contains the guides, issue log, index,
questions and screenshots. The empty `Docs/` directory, root question path and
duplicate root screenshot path are gone. Viewer implementation, documentation
and test changes are ready for the authorized commit and push.

## Blockers
Actual Chrome rendering remains a separate open verification item. It does not
prevent path and link housekeeping.

## Next Steps
Review the staged diff, commit the complete verified change, and push it to
`origin/main`. Keep the browser verification item open until a Browser
connection is restored.

## Key Files
`docs/_INDEX.md`, `docs/user-guide.md`, `docs/known_issues.md`,
`docs/questions/`, `README.md`, `AGENTS.md`, `work/action-plan.md`.

## Scope Check
Only documentation paths, references and navigation are in this housekeeping
scope. Viewer behaviour, schema semantics, private sibling-workspace files,
and the browser verification gate remain unchanged.
